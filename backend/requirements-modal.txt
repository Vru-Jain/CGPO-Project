# Container image requirements for the Modal deployment (backend/modal_app.py).
# Deliberately a SUBSET of requirements.txt: only what the FastAPI serving path
# actually imports. The local-only / unused deps (streamlit, matplotlib, plotly,
# selenium, gspread, oauth2client, google-auth) are excluded so the image is
# smaller and cold starts pull faster. torch stays on the default CUDA wheel
# because the function runs on a T4 GPU.
pandas>=2.0.0
numpy>=1.24.0
networkx>=3.0
torch>=2.0.0
torch_geometric>=2.4.0
textblob>=0.17.1
yfinance>=0.2.36
ta>=0.11.0
gymnasium>=0.29.0
fastapi>=0.109.0
uvicorn>=0.25.0
pydantic>=2.0.0
python-multipart>=0.0.6
